<main>

    <div class="body">

        <div class="card">

            <div class="divAsideL">
                <div class="img">{{book_img}}</div>

                <button class="author">{{books.author}}</button>
                <button class="Publisher">{{books.publisher}}</button>
                <button class="Genre">{{books.genre}}</button>
                <button class="addToCart">{{Add To Cart}}</button>
                <button class="Favorite">{{Favorite}}</button>

            </div>

            <div class="divCenter">

                <div class="bookName">{{books.title}} <br>

                    {{#if books.series}}
                    <div class="bookSeries">{{books.series}}</div>
                    {{/if}}
                    <div class="inventory">{{books.quantity}}</div>
                    <div class="price">{{books.price}}</div>
                </div>



                <div class="bookDescription">{{books.description}}</div>
            </div>


        </div>

        <div class="review">
            <div class="reviewPosts">{{book_reviews}}</div>
            <input type="text" name="postAReview" id="reviewPost" placeholder="Write a Review" />
        </div>

    </div>
    <div class="related">

        <div class="relatedHeader">Related Books</div>

        <div class="relatedSearch">

            {{#each book.related as |book|}}
            {{> bookMiniCard}}
            {{/each}}

        </div>
    </div>

</main>